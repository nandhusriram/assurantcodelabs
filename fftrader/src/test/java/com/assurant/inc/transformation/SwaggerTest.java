package com.assurant.inc.transformation;


public class SwaggerTest {

	/*@Test
	public void annotaionPresent_Api() 
	{
		// when
		Iterable<Class<?>> annotated = ClassIndex.getAnnotated(Api.class);
		for (Class<?> class1 : annotated) {
			System.out.println(class1);
		}
		// then
		assertThat(annotated).containsOnly(TimeServiceEndpoint.class,IndexEndpoint.class);

	}
	
	@Test
	public void annotaionPresent_Path() 
	{
		// when
		Iterable<Class<?>> annotated = ClassIndex.getAnnotated(Path.class);
		for (Class<?> class1 : annotated) {
			System.out.println(class1);
		}
		// then
		assertThat(annotated).containsOnly(TimeServiceEndpoint.class,IndexEndpoint.class);

	}
*/
}
